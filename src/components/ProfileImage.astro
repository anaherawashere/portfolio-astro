---
interface Props {
  size: string
}

const { size } = Astro.props
---

<div class="relative">
  <div class={`overflow-hidden max-w-${size} w-${size} h-${size} rounded-full`}>
    <img
      src="/images/anahera.jpeg"
      alt="Anahera"
      class="object-cover"
    />
  </div>
</div>
